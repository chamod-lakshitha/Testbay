import React from 'react';
import ReactDOM from 'react-dom/client';
import AppRouts from './components/app_routes';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <>
    <AppRouts />
  </>
);
